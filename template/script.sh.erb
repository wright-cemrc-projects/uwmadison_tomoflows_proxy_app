#!/bin/bash

# These need to be setup to match a local installation of TomoFlows
cd $HOME
APP_DIR=github/TomoFlows
cd $APP_DIR

# source the python environment
# start the application on the correct ports

echo "Starting TomoFlows on port ${port}"
source env/bin/activate
python manage.py runserver 0.0.0.0:${port}
